import React from 'react';
import Form from '@components/Form';
import Input from '@components/Input';

const Page = () => {
  return (
    <Form>
      <Input />
    </Form>
  );
};

export default Page;